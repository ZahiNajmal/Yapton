fanumtax math

num1 = spill("Enter a number: ")

is_positive = num1 > 0   # this is W (True) or L (False)

if is_positive == L:
    yap("The number you gave is negative number")
otherwise:
    yap("The number you gave is positive number")